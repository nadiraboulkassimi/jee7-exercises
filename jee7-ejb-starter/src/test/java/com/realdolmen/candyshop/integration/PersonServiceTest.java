package com.realdolmen.candyshop.integration;

import static org.junit.Assert.assertEquals;

import org.junit.Before;
import org.junit.Test;

import com.realdolmen.candyshop.AbstractRemoteIntegrationTest;
import com.realdolmen.candyshop.repository.CandyRepository;

public class PersonServiceTest extends AbstractRemoteIntegrationTest{
/*
	 @Before
	    public void initializeRepository() {
	        repository = new CandyRepository();
	        repository.em = em;
	    }

	    @Test
	    public void findAverageCandyPrice() throws Exception {
	        double avg = repository.findAverageCandyPrice();
	        assertEquals(5.15, avg, 0.01);
	    }
	    */
}
